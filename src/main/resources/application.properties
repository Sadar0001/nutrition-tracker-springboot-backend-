spring.application.name=diet-tracker

# -----------------------------------------------------------
# DATABASE CONFIGURATION (TiDB Serverless)
# -----------------------------------------------------------
# Your exact TiDB connection string with required SSL flags
spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY&enabledTLSProtocols=TLSv1.2,TLSv1.3

# Your exact TiDB username
spring.datasource.username=4Fhuoak8Fi1DBqd.root

# REPLACE "hidden" WITH YOUR ACTUAL TIDB PASSWORD
spring.datasource.password=qnq8U1oBROG77Eh0

# Standard MySQL driver works perfectly with TiDB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -----------------------------------------------------------
# JPA / HIBERNATE SETTINGS
# -----------------------------------------------------------
# 'update' automatically creates your users and food_logs tables!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# -----------------------------------------------------------
# FRONTEND CONNECTION (CORS)
# -----------------------------------------------------------

# -----------------------------------------------------------
# SECURITY (CLERK)
# -----------------------------------------------------------
# CRITICAL: Replace the bracketed text below with your real Clerk URL!
# Find this in Clerk Dashboard -> API Keys -> Advanced -> JWT Templates
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://allowing-monarch-92.clerk.accounts.dev
ai.service.url=http://127.0.0.1:8000
app.frontend.url=${FRONTEND_URL:http://localhost:5173}